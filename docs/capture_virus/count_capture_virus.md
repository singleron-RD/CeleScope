

## Arguments
`--min_query_length` minimum query length.

`--outdir` output dir.

`--assay` assay.

`--sample` sample name.

`--thread` None

`--debug` debug.

`--match_dir` matched rna_virus directory.

`--virus_bam` None

