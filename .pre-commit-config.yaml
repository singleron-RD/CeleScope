repos:
  
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Ruff version.
    rev: v0.4.4
    hooks:
      # Run the linter.
      - id: ruff
        args: [ "--fix", "--ignore=E711,E712" ]
      # Run the formatter.
      - id: ruff-format